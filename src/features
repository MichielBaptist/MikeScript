Structural pattern matching:

type enum {
    node(int),
    nodeExp(int, int),
}

node >>= match {
    nodeExp(x, y) => x +y,
    node(z) => z * 2,
}



Generic types for functions:

function<T> (T x, T y) >> f -> int {
    return x + y;
}